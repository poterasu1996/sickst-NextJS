export interface IUserInfo {
    blocked: boolean,
    client_role: string,
    confirmed: boolean,
    createdAt: string,
    email: string,
    id: number,
    new_user: boolean,
    newsletter: boolean,
    provider: string,
    subscribed: boolean,
    updatedAt: string
    username: string
}