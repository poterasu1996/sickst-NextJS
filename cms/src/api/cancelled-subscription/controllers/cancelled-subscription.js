'use strict';

/**
 *  cancelled-subscription controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cancelled-subscription.cancelled-subscription');
