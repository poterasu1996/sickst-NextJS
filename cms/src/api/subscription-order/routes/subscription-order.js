'use strict';

/**
 * subscription-order router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::subscription-order.subscription-order');
