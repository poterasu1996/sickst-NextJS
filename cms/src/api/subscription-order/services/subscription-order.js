'use strict';

/**
 * subscription-order service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-order.subscription-order');
