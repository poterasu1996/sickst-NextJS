'use strict';

/**
 *  cancelled-order controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cancelled-order.cancelled-order');
